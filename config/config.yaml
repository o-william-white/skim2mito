# path to sample sheet csv with columns for ID,forward,reverse
samples: config/samples.csv

# name of output directory
output_dir: results

# path to blast db
blast_db: </path/to/blastdb>

# path to new_taxdump
taxdump: </path/to/new_taxdump/>

# path to mitos refseq
mitos_refseq: </path/to/mitos/refseq>

# mito code
mitos_code: <code>

# number of threads to use
threads: <threads>

